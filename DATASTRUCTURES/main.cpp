#include <iostream>
using namespace std;

int main()
{
	int choice;
	swith(true)
	{
		cout<<"1) Element inside at back";
		cout<<"2) Element delete at back";
		cout<<"3) Element insert at begin";
		cout<<"4) Element delete at begin";
		cout<<"5) Display the Array";
		
		cin>>choice;
	}
	
}